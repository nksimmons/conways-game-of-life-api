Notes to keep in mind

General goal
- Implement a RESTful API for Conway's Game of Life. Your solution should be designed with production readiness in mind.

RESTful API
- Use proper verbs
- Should we use HATEOAS?
-- If so, how do we avoid the N+1 problem?
- Endpoints
-- Upload Board State
--- POST or PUT?
-- Get Next State
-- Get N States Ahead
--- What does the path look like?
-- Get Final State
--- Same endpoint?

Non-Functional Requirements
- Persist board states such that they are not lost if app is restarted or crashes
-- Integration test?
- Production-ready
-- Clean, modular, testable
--- Does Clean = Uncle Bob?
--- Testable
---- IoC container
---- Unit tests
---- Integration tests
---- Functional tests
---- Load tests
- C# and .NET best practices
-- TPL
-- Non-blocking
-- Don't fill up thread queue
-- Don't do async void
-- Error on the side of request or transient scope
-- Inject HttpClients as singletons
-- Concurrency
--- Use bounded concurrency
---- Channels or concurrent queue, if warranted
--- Avoid coarse grain locking, if possible
--- Concurrent dictionary is preferred is the data structure is warranted
--- Semaphore slim next
--- Mutex if absolutely needed
- Repository pattern
-- Don't put a repository on top of EF which already uses the repository pattern

Open Questions and Things to Remember
- Should we use a game engine to manage the business logic a grid?
- Should we introduce an abstraction on top of the grid because my ADHD brain has a hard time reasoning over raw indexing?
-- Might be good to introduce some domain modeling
- Anemic domain model or rich (probably the latter)?
- Should we use a dockerfile and spin up multiple containers?
- Should we use a queue?
- How should we store state?
-- Is it best modeled relationally?
--- If so, use relational DB
--- If not, use some other data store
- Is the flow write heavy or read heavy?
-- If relationally, postgres (MVCC) or SQL Server?
--- Don't forget indexes, stored procs vs. prepared statements, query plans
-- If not relational, and write heavy, should we use a columnar data store with a write ahead log?
--- How should we design the schema for querying?
- Should we use CQRS?
- Caching?
-- Use IDistributedCache with an in-memory provider for local?
-- Should we spin up redis?
- Should we emulate union types since .NET 8 doesn't have them?
- We should probably err on the side of doing things on a single node but use an abstraction that allows us to distribute
- Should we use a micro-ORM or EF?
- Database-first or code-first (I prefer DB)?
- Don't forget logging
- Don't forget to guard against invalid inputs

Action items:
- Write up a design doc
-- Include decisions and rationales
-- Include diagrams

